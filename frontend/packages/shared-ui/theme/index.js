import { colors, spacing, typography, radius, shadows, animation, zindex } from '../tokens';

export const theme = {
  colors,
  spacing,
  typography,
  radius,
  shadows,
  animation,
  zindex
};

export default theme;
